# AIMC 数字人主持系统 - 依赖声明

# 核心依赖
pyaudio >= 0.2.13
numpy >= 1.21.0
scipy >= 1.7.0
scikit-learn >= 1.0.0

# 语音处理
SpeechRecognition >= 3.8.1
pydub >= 0.25.1

# 自然语言处理
spaCy >= 3.2.0
jieba >= 0.42.1
nltk >= 3.6.5

# 音频处理
librosa >= 0.9.1

# 配置管理
pyyaml >= 6.0
toml >= 0.10.2

# 数据处理
pandas >= 1.3.0
openpyxl >= 3.0.0

# 可视化
matplotlib >= 3.4.0
seaborn >= 0.11.0

# 工具库
requests >= 2.26.0
tqdm >= 4.62.0
python-dotenv >= 0.19.0

# 调试和测试
pytest >= 6.2.0
pytest-cov >= 2.12.0
mock >= 4.0.3

# 建议安装
# 对于不同的TTS和语音识别服务，可能需要额外的依赖
# AWS Polly: boto3
# Azure TTS: azure-cognitiveservices-speech
# Baidu Speech: baidu-aip
# Google Speech: google-cloud-speech

# 可选依赖（根据需要）
# 用于高级音频处理
# noisereduce >= 2.0.0
# soundfile >= 0.10.3.post1

# 用于WebSocket通信
# websockets >= 10.0
